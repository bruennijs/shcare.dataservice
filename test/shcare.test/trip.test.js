/**
 * Created by bruentje on 26.10.13.
 */

var q = require('q'),
  mocha = require('mocha'),
  should = require('should');

describe('osjd', function() {
  it ('should true', function () {
    var num = 2;
    num.should.be.equals(2);
  })
})

describe("test suite", function () {
  it("should use Q promise framework in browser", function() {
  })
});
